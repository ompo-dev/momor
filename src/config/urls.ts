/**
 * Centralised checkout & external URL constants.
 *
 * Checkout URLs have been removed in the open-source build.
 * Payments via Dodo Payments are no longer supported.
 */

export const CHECKOUT_URLS = {
    /** Disabled in open-source build */
    pro: '',
    /** Disabled in open-source build */
    apiStandard: '',
    /** Disabled in open-source build */
    apiPro: '',
    /** Disabled in open-source build */
    apiMax: '',
    /** Disabled in open-source build */
    apiUltra: '',
} as const;
